import 'package:flutter/material.dart';

// ignore: camel_case_types
class policies extends StatelessWidget {
  const policies({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Policics"),
      ),
    );
  }
}
